/**
 * Canonical production origin. Used for metadataBase, canonical URLs, the
 * sitemap, robots, and structured data. Update if the production domain
 * changes. The current Vercel preview is a separate deployment target.
 */
export const SITE_URL = "https://www.lodestoneglobal.com";

export const SITE_NAME = "Lodestone Global";
